# Direct dependencies for the JWST MAST dataset builder.
# Pinned to the versions verified working on Python 3.11.
# (astropy, pyvo, requests, etc. are pulled in automatically by astroquery.)
astroquery==0.4.11
pandas==3.0.3
numpy==2.4.6
